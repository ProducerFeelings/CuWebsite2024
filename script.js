// Show password functionality function myFunction() { var x = document.querySelector('#signin #password'); if (x.type === "password") { x.type = "text"; } else { x.type = "password"; } } function myFunction2() { var passwordField = document.querySelector('#signup #password'); var showPasswordCheckbox = document.getElementById("show-password-checkbox"); if (showPasswordCheckbox.checked) { passwordField.type = "text"; // Show the password } else { passwordField.type = "password"; // Hide the password } } // Password validation logic function validatePassword(password) { const minLength = 8; const hasUpperCase = /[A-Z]/.test(password); const hasLowerCase = /[a-z]/.test(password); const hasNumber = /[0-9]/.test(password); const hasSpecialChar = /[!@#$%^&*(),.?":{}|<>]/.test(password); // Avoid common passwords or easily guessable sequences const commonPasswords = ['password', '123456', '123456789', 'qwerty', 'abc123']; if (commonPasswords.includes(password.toLowerCase())) { return "Password is too common. Please choose a different password."; } // Avoid using easily accessible personal information const personalInfo = [document.getElementById('signin-username').value, document.getElementById('signup-username').value]; if (personalInfo.includes(password)) { return "Password should not contain easily accessible personal information."; } // Avoid repeated characters or sequences if (/(\w)\1{2,}/.test(password)) { return "Password should not contain repeated characters."; } if (password.length < minLength) { return "Password must be at least 8 characters long."; } if (!hasUpperCase) { return "Password must contain at least one uppercase letter."; } if (!hasLowerCase) { return "Password must contain at least one lowercase letter."; } if (!hasNumber) { return "Password must contain at least one number."; } if (!hasSpecialChar) { return "Password must contain at least one special character."; } return ""; // Password is valid } // Form submissions document.addEventListener('DOMContentLoaded', function() { // Get modals var signinModal = document.getElementById('signin'); var signupModal = document.getElementById('signup'); var forgotPasswordModal = document.getElementById('forgot1'); // Get open modal links var openSigninLink = document.querySelector('a[href="#signin"]'); var openSignupLink = document.getElementById('open-signup'); var openForgotPasswordLink = document.querySelector('a[href="#forgotpassword"]'); // Get close buttons var closeSigninButton = document.getElementById('close-signin'); var closeSignupButton = document.getElementById('close-signup'); var closeForgotPasswordButton = document.getElementById('close-forgot-password'); // Function to open a modal function openModal(modal) { modal.style.display = 'block'; } // Function to close a modal function closeModal(modal) { modal.style.display = 'none'; } // Event listeners for opening modals openSigninLink.addEventListener('click', function(e) { e.preventDefault(); openModal(signinModal); }); openSignupLink.addEventListener('click', function(e) { e.preventDefault(); openModal(signupModal); }); openForgotPasswordLink.addEventListener('click', function(e) { e.preventDefault(); openModal(forgotPasswordModal); }); // Event listeners for closing modals closeSigninButton.addEventListener('click', function() { closeModal(signinModal); }); closeSignupButton.addEventListener('click', function() { closeModal(signupModal); }); closeForgotPasswordButton.addEventListener('click', function() { closeModal(forgotPasswordModal); }); // Form submissions document.getElementById('signin-form').addEventListener('submit', function(event) { event.preventDefault(); const password = document.getElementById('password').value; const validationMessage = validatePassword(password); if (validationMessage) { alert(validationMessage); } else { alert("Sign In Successful"); } }); document.getElementById('signup-form').addEventListener('submit', function(event) { event.preventDefault(); const password = document.getElementById('password').value; const validationMessage = validatePassword(password); if (validationMessage) { alert(validationMessage); } else { alert("Sign Up Successful"); } }); });
